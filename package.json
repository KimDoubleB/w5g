{
  "name": "w5g",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "rm -rf .tmp-test && tsc -p tsconfig.test.json && node --test .tmp-test/naverMap.test.js",
    "preview": "vite preview",
    "resize-images": "node scripts/resize-images.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "sharp": "^0.34.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
